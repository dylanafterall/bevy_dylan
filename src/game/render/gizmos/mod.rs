use bevy::prelude::*;

// -----------------------------------------------------------------------------
pub struct GizmosPlugin;

impl Plugin for GizmosPlugin {
    fn build(&self, app: &mut App) {
        // app.add_plugins((,));
    }
}
