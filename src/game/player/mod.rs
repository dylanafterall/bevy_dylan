use bevy::prelude::*;

pub struct PlayerPlugin;

impl Plugin for PlayerPlugin {
    fn build(&self, app: &mut App) {
        app
            // resources
            // plugins
            // states
            // events

            // systems: startup
            /*
            .add_systems(Startup, (

            ));
             */
    }
}