use bevy::prelude::*;

pub struct CharacterPlugin;

impl Plugin for CharacterPlugin {
    fn build(&self, app: &mut App) {
        app
            // resources
            // plugins
            // states
            // events

            // systems: startup
            /*
            .add_systems(Startup, (

            ));
             */
    }
}