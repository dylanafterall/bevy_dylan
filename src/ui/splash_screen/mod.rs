use bevy::prelude::*;

// -----------------------------------------------------------------------------
pub struct SplashPlugin;

impl Plugin for SplashPlugin {
    fn build(&self, app: &mut App) {
        app
            .add_plugins((
                ));
    }
}